#pragma strict

var startPos: Vector3;
var thing: GameObject;
var endPos: Vector3;
var speed: int;
var flip: boolean;

function Awake()
{
	startPos = transform.position;
	endPos = thing.transform.position;
}

function Update()
{
	if(flip)
	{
		
	}
	
	if(flip ==  false)
	{
		
	}
}